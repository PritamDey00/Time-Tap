# Environment Variables Configuration
# Copy this file to .env.local for local development

# =============================================================================
# REQUIRED FOR ALL ENVIRONMENTS
# =============================================================================

# JWT Secret for token signing and verification
# SECURITY: Use a strong, random secret (minimum 32 characters)
# Generate with: openssl rand -base64 32
# Use different secrets for development and production
JWT_SECRET=super-secret-change-me-to-a-secure-random-string

# =============================================================================
# PRODUCTION ONLY - UPSTASH REDIS CONFIGURATION
# =============================================================================
# These variables are ONLY required when NODE_ENV=production
# For local development, the app uses filesystem storage automatically

# Upstash Redis REST API URL
# Get from: Upstash Dashboard → Your Database → REST API
# Format: https://your-redis-instance.upstash.io
# UPSTASH_REDIS_REST_URL=https://your-redis-instance.upstash.io

# Upstash Redis REST API Token
# Get from: Upstash Dashboard → Your Database → REST API
# Keep this token secure - it provides full database access
# UPSTASH_REDIS_REST_TOKEN=AXXXaGVsbG8gZnJvbSB1cHN0YXNoIQ

# =============================================================================
# SYSTEM VARIABLES (Automatically set by deployment platforms)
# =============================================================================
# NODE_ENV=development  # Local development (uses filesystem storage)
# NODE_ENV=production   # Production deployment (uses Redis storage)

# =============================================================================
# SETUP INSTRUCTIONS
# =============================================================================
# 
# FOR LOCAL DEVELOPMENT:
# 1. Copy this file: cp .env.example .env.local
# 2. Update JWT_SECRET with a secure random string
# 3. Redis variables are NOT needed - app uses filesystem storage
# 
# FOR PRODUCTION (Vercel):
# 1. Create Upstash Redis account at https://upstash.com
# 2. Create a new Redis database
# 3. Get REST URL and Token from Upstash dashboard
# 4. Set environment variables in Vercel dashboard:
#    - JWT_SECRET (secure production secret)
#    - UPSTASH_REDIS_REST_URL
#    - UPSTASH_REDIS_REST_TOKEN
# 5. Deploy your application
#
# For detailed setup instructions, see: docs/ENVIRONMENT_VARIABLES.md