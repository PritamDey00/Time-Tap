# Environment variables for local development
# This file is for development only - never commit production secrets!

# JWT Secret for development
# This is a development-only secret - use a different one for production
JWT_SECRET=dev-secret-classroom-platform-local-development-key-2024

# Development environment (automatically detected)
# NODE_ENV=development

# Redis variables are NOT needed for development
# The application automatically uses filesystem storage in development mode
# Data is stored in local JSON files in the data/ directory

# For production deployment instructions, see:
# - docs/ENVIRONMENT_VARIABLES.md
# - .env.example